# Euskalmet 2.9.0-beta.2

- Declara `CONFIG_SCHEMA` mediante
  `cv.config_entry_only_config_schema(DOMAIN)`.
- Indica explícitamente que Euskalmet se configura exclusivamente mediante
  entradas de configuración y no admite configuración YAML.
- Actualiza la versión de caché de los tres recursos frontend.
